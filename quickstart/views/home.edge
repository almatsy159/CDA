<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/users">Formulaire</a></li>
    </ul>
    <h1>Hello World!</h1>
    <table>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Job</th>
            <th>Actions</th>
        </tr>
        @each ((user,index) in users)
        <tr>
            <th>{{ index }}</th>
            <th>{{ user.name }}</th>
            <th>{{ user.job }}</th>
            <th>
                <form method="get" action="/users/{{index}}">
                    <input type="submit" name="edit" id="edit" value="✏️">
                </form>
                <form method="get" action="/users/{{index}}/delete2">
                    <input type="submit" name="delete" id="delete" value="🗑️">
                </form> 
            </th>
        </tr>
        @endeach
    </table>
    <ul>
        <!--
        @each((user,index) in users)
        <li><a href="/users/{{index}}">{{ user.name }} - {{ user.job }}</a>
            <form method="get" action="/users/{{index}}/delete2">
                <input type="submit" value="X">
            </form>
        </li>
        @endeach
    -->
    </ul>
</body>

</html>